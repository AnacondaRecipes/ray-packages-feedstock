gpu_variant:
  - cuda-13

cuda_compiler_version:
  - 13

zip_keys:
  -
    - gpu_variant
    - cuda_compiler_version

cudnn:                                            # [linux and x86_64]
  - undefined                                     # [linux and x86_64]